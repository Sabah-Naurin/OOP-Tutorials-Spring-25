package com.animalproject;

public class Animal {
    String name, breed;
    int age;

    public String printAnimalDetails() {
        return "The animal is " + breed + ". It's name is " + name + ". It's age is " + age + ".";
    }
}
